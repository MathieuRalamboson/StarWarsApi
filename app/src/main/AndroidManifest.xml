<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mathieu.r">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.DetailObjectActivity"></activity>
        <activity android:name=".View.VehiclesActivity" />
        <activity android:name=".View.StarshipsActivity" />
        <activity android:name=".View.SpeciesActivity" />
        <activity android:name=".View.PlanetsActivity" />
        <activity android:name=".View.PeopleActivity" />
        <activity android:name=".View.FilmActivity" />
        <activity android:name=".View.CategorieActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>